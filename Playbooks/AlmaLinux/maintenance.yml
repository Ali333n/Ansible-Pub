---
- name: Full Almalinux maintenance with disk space check and updates
  hosts: all
  vars:
    ansible_python_interpreter: /usr/bin/python3
  gather_facts: true

  roles:
    - role: check-diskspace
    - role: update-packages
